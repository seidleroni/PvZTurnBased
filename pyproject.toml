[project]
name = "garden-guard"
version = "0.1.0"
description = "A calm turn-based garden tactics game for kids."
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "playwright>=1.57.0",
]

[tool.uv]
package = false
